/**
 * Application-wide constants
 */

/**
 * Module completion threshold (0-1 scale)
 * Students must achieve this accuracy on all module quizzes to unlock the next module
 * @default 0.85 (85%)
 */
export const MODULE_COMPLETION_THRESHOLD = 0.85;

/**
 * Minimum percentage required to consider a module's content "completed"
 * @default 1.0 (100% of content items must be completed)
 */
export const CONTENT_COMPLETION_THRESHOLD = 1.0;

